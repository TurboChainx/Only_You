# Production Android App Bundle

The latest **production** `.aab` from EAS is saved here for local handoff (email, Drive, etc.).

| File | Description |
|------|-------------|
| `OnlyYou-1.0.0-production.aab` | Android App Bundle (Google Play). Built with `eas build --platform android --profile production`. |

`*.aab` files are listed in `.gitignore` and are **not** committed to Git. Regenerate or re-download from Expo if needed.

See **`docs/LATEST_PRODUCTION_BUILD.md`** for the build page link and artifact URL.
