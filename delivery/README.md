# Production Android App Bundle

The latest **production** `.aab` from EAS is saved here for local handoff (email, Drive, etc.).

| File | Description |
|------|-------------|
| **`OnlyYou-1.0.1-production.aab`** | Current bundle — **version 1.0.1** (versionCode **2**), package **`com.onlyyouchat.app`**. |
| `OnlyYou-1.0.0-production.aab` | Older build (`com.onlyyou.app`); do not use for the new Play listing. |

`*.aab` files are listed in `.gitignore` and are **not** committed to Git.

See **`docs/LATEST_PRODUCTION_BUILD.md`** for the latest Expo build page and download link.
